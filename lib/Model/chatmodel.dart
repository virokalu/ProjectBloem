class ChatModel {
    String name;
    String time;
    String currentpage;
    String img;
    int id;
    ChatModel({required this.name,required this.time,required this.currentpage,required this.img,required this.id});
}